# GENERATED CODE. DO NOT MODIFY
import numpy as np
import time
import keyboard
import math
from PythonImageArt.interpreter import util
SIZE = (1024, 1024)
image = util.get_blank_array(SIZE)
print("DRAWING...")
for x in np.arange(SIZE[0]):
    for y in np.arange(SIZE[1]):

print("FINISHED DRAWING")
util.quick_show(image)
print('PRESS "S" TO SAVE AND "N" TO SKIP')
while 1:
    if keyboard.is_pressed('s'):
        name = f"{time.time()}out.png"
        util.save_array_as_img(name, image)
        print(f"SAVED AS {name}")
        break
    if keyboard.is_pressed('n'):
        print("NO SAVE")
        break
print("FINISHED")
